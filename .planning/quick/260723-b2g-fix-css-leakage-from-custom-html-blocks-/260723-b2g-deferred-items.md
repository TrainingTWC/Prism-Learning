# Deferred items — Quick Task 260723-b2g

- **Pre-existing lint issue (out of scope):** `apps/web/src/lib/scormExport.ts`, `sorting` block-rendering case, has a `no-useless-escape` ESLint warning. Confirmed present in the pre-dispatch base commit (`ff3430a`), unrelated to this fix. Not touched.
