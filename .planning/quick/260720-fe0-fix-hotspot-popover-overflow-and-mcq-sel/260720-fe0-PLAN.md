---
phase: quick-260720-fe0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/renderer/src/HotspotsBlockRenderer.tsx
  - packages/renderer/src/MCQBlockRenderer.tsx
autonomous: false
requirements: [QUICK-260720-fe0]

must_haves:
  truths:
    - "An open hotspot popover stays fully inside the image container on any viewport width (no horizontal clipping)."
    - "Selecting an MCQ option before submit shows a filled indicator (dot for radio, check for checkbox), never a correct/incorrect ✓/✗."
    - "After submit, selected MCQ options still show ✓ (correct) or ✗ (incorrect) as before."
  artifacts:
    - path: "packages/renderer/src/HotspotsBlockRenderer.tsx"
      provides: "Bottom-pinned, side-inset hotspot popover that cannot overflow horizontally"
    - path: "packages/renderer/src/MCQBlockRenderer.tsx"
      provides: "Neutral pre-submit selection marker + post-submit correctness reveal"
  key_links:
    - from: "HotspotsBlockRenderer popover div"
      to: "overflow:hidden container"
      via: "position:absolute left/right/bottom insets (no xPct-based left/transform)"
      pattern: "right:|bottom:"
    - from: "MCQBlockRenderer marker span"
      to: "submitted / isSelected / multiSelect state"
      via: "conditional marker content"
      pattern: "submitted"
---

<objective>
Fix two learner-preview renderer bugs:
1. Hotspot popover leaks/clips out of view on narrow viewports.
2. MCQ selection marker shows an empty ring and prematurely reveals correctness before submit.

Purpose: These are visible correctness/usability defects in the learner preview renderers that undermine the authoring quality bar.
Output: Corrected `HotspotsBlockRenderer.tsx` and `MCQBlockRenderer.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/renderer/src/HotspotsBlockRenderer.tsx
@packages/renderer/src/MCQBlockRenderer.tsx

<notes>
- HotspotsBlockRenderer uses INLINE styles only (no Tailwind). Match that idiom.
- MCQBlockRenderer uses Tailwind utility classes + a small inline style object. Match that idiom.
- These two files are the ONLY files in scope. Do NOT touch other block renderers, SCORM export, or editor components.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-anchor hotspot popover to bottom, side-inset (no horizontal overflow)</name>
  <files>packages/renderer/src/HotspotsBlockRenderer.tsx</files>
  <action>
In the `{open && (...)}` popover block (~lines 53-78), change the outer popover `<div>` inline style so it is pinned near the bottom of the image container and spans the width with side insets, instead of being placed beside the dot.

Specifically, in the style object (~lines 55-65):
- REMOVE `left: \`${open.xPct}%\``.
- REMOVE the `transform: \`translate(${open.xPct > 50 ? ...})\`` line entirely.
- REMOVE `top: \`${open.yPct}%\``.
- REMOVE `maxWidth: 280` and `minWidth: 200`.
- ADD `left: 12`, `right: 12`, `bottom: 12` so the card is inset from both sides and pinned to the bottom of the `overflow:hidden` container.
- KEEP `position: 'absolute'`, `background: '#fff'`, `color: '#1a1a2e'`, `borderRadius: 12`, `padding: 16`, the `boxShadow`, and `zIndex: 5` exactly as they are.

Leave everything else untouched: the numbered hotspot dot buttons (~lines 30-52), the close (×) button, the title `<strong>`, the body `<p>`, the pulse `<style>` keyframes, and the parent container. Do NOT remove or change `overflow: 'hidden'` on the parent — the fix relies on it clipping nothing because the card now sits fully inside.
  </action>
  <verify>
    <automated>npx tsc -p packages/renderer/tsconfig.json --noEmit 2>&1 | grep -v '^#' | grep -i 'HotspotsBlockRenderer' | grep -c error | grep -qx 0 && echo TYPE_OK</automated>
    <human-check>In learner preview, open a module with a hotspots block on a narrow (phone-width) viewport. Click a hotspot dot near the left and right image edges. Confirm the popover card stays fully inside the image with no text cut off on either side.</human-check>
  </verify>
  <done>Popover style uses `left:12 / right:12 / bottom:12` with no `xPct`-based `left`/`transform`/`top` and no fixed `maxWidth`/`minWidth`; TypeScript compiles clean; card cannot overflow horizontally on any viewport width.</done>
</task>

<task type="auto">
  <name>Task 2: Fix MCQ selection marker — neutral pre-submit fill, no premature correctness reveal</name>
  <files>packages/renderer/src/MCQBlockRenderer.tsx</files>
  <action>
Replace ONLY the marker span content expression at ~line 108, currently `{isSelected ? (correct ? '✓' : '✗') : ''}`, with logic that:
- If `submitted && isSelected` → render `correct ? '✓' : '✗'` (existing post-submit reveal — keep exactly).
- Else if `!submitted && isSelected && multiSelect` (checkbox) → render a neutral `'✓'` (selected, not graded; inherits the existing `border-current` / text color, no correctness meaning).
- Else if `!submitted && isSelected && !multiSelect` (radio) → render a filled inner dot: an inline child `<span>` roughly 10px square with `borderRadius: '50%'` and `background: 'currentColor'` (use an inline style object for those three properties, matching the file's existing pattern of Tailwind classes + small inline style objects). This makes the radio read as filled.
- Else (not selected) → render nothing (empty).

The premature `✓`/`✗` correctness reveal before submit MUST be gone.

Do NOT change: the marker span's className (the `size-5`, `rounded` vs `rounded-full` switch, `border-2`, `border-current` vs `border-slate-300`, `prism-marker-pop`, text sizing); the option `optClass`/`optStyle` correct/incorrect background logic (~lines 82-93); `correctStyle`/`incorrectStyle`; the submit/reset controls; or the feedback paragraph.
  </action>
  <verify>
    <automated>npx tsc -p packages/renderer/tsconfig.json --noEmit 2>&1 | grep -v '^#' | grep -i 'MCQBlockRenderer' | grep -c error | grep -qx 0 && echo TYPE_OK</automated>
    <automated>grep -c "correct ? '✓' : '✗'" packages/renderer/src/MCQBlockRenderer.tsx | grep -qx 1 && echo REVEAL_GATED_ONCE</automated>
    <human-check>In learner preview: (1) single-select MCQ — click an option; the radio shows a filled inner dot (not empty, not ✓/✗). (2) multi-select MCQ — click options; each shows a neutral ✓ with no red ✗. (3) Submit — selected options now reveal ✓ (correct) / ✗ (incorrect) as before.</human-check>
  </verify>
  <done>Pre-submit selection shows a filled dot (radio) or neutral ✓ (checkbox) and never ✓/✗ correctness; post-submit reveal unchanged; the single `correct ? '✓' : '✗'` expression is gated behind `submitted && isSelected`; TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
No new trust boundaries. Both changes are presentational edits to pure, dependency-injected renderer components (no new inputs, network calls, package installs, or `dangerouslySetInnerHTML` sites — existing sanitized HTML sinks are untouched). No STRIDE-relevant surface introduced.
</threat_model>

<verification>
- `npx tsc -p packages/renderer/tsconfig.json --noEmit` passes with no new errors.
- Hotspot popover stays inside the image container at phone width near both edges.
- MCQ pre-submit selection shows filled markers with no correctness leak; post-submit reveal intact.
</verification>

<success_criteria>
- Both files match their existing idioms (inline styles for Hotspots; Tailwind + small inline style object for MCQ).
- No other renderers, SCORM export, or editor components touched.
- All three `must_haves.truths` hold.
</success_criteria>

<output>
Create `.planning/quick/260720-fe0-fix-hotspot-popover-overflow-and-mcq-sel/260720-fe0-SUMMARY.md` when done.
</output>
