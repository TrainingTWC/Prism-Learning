// Convex Auth wiring — Phase 1 will replace this stub with the real
// @convex-dev/auth magic-link provider configuration (Resend or SES).
//
// See: https://labs.convex.dev/auth
//
// Intentionally a placeholder so `npx convex dev` can run after install
// without forcing the user to pick an email provider on day zero.

export const placeholder = true;
